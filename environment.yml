name: volumePlot
channels:
  - defaults
dependencies:
  - python=3.7
  - matplotlib
  - numpy
  - xarray=0.16.1
  - scipy
  - netcdf4
  - ipykernel
  - scikit-image
prefix: /opt/anaconda3/envs/volumePlot
